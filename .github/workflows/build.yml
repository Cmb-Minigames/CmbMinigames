# Generated by ActionFlow

# Project Name: Build minecraft plugin
# Project Description: Use gradlew to run the build gradle task and upload the jar file as an artifact

# Components that aren't connected to actions
# will NOT be displayed!

name: Build minecraft plugin

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Grant access to execute gradlew
        run: |
          chmod +x gradlew

      - name: Build the jar
        run: |
          ./gradlew build

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Plugin
          path: build/libs/*.jar